@model IEnumerable<CourseworkApp.Models.ConferenceCallResearcher>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ConferenceCallID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ConferenceCallID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ResearcherID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ResearcherID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ResearcherID })
        </td>
    </tr>
}

</table>
