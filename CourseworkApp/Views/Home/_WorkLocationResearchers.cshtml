@model IEnumerable<CourseworkApp.Models.Researcher>
@foreach(var item in Model)
{
    @foreach (var result in ViewBag.Results)
    {
        //Find out how to print and write if statment
        ViewBag.result;
    }
}